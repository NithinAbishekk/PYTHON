a = 3
b = 4
print(a&b) # bitwise and operator - it compares the bits of the two numbers and returns a new number where the bits are set to 1 only if both corresponding bits of the operands are 1, so in this case, the result will be 0 because there are no bits that are set to 1 in both numbers.