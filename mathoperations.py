import math

a = 46.34
b = -5
c = 98.5
d = 98.5
x = 5.46

#print(math.ceil(x)) # to round up a number, which means that it will round the number up to the nearest integer, so in this case it will round 5.46 up to 6.
#print(math.floor(x)) # to round down a number

#print(round(a)) # rounding off a number
#print(abs(b)) # absolute value
# print(pow(a,3))# to find the cube of a number, which is the same as a**3, but using pow() function is more concise and easier to read.
# print(a**3) # cube of a
# print(a**2) # square of a

# #to find the maximum and minimum value
#print(max(c,d)) 
#print(min(c,d))

